{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd ai_ops_assistant && uvicorn api_server:app --host 0.0.0.0 --port $PORT"
  }
}
